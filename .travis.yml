language: generic

dist: trusty

services:
  - docker

install:
  - docker pull lamtev/build-tools

script:
  - docker run -v /home/travis/build/lamtev/Go:/sources lamtev/build-tools /bin/bash -c "cd sources && ./travis_build.sh"

language: generic

dist: trusty

services:
  - docker

install:
  - docker pull lamtev/build-tools

script:
  - sudo chmod 755 travis_build.sh
  - DIR=$(pwd)
  - docker run -v $DIR lamtev/build-tools /bin/bash -c "./travis_build.sh"
